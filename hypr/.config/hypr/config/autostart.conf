# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                    Autostart Configuration                  ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

source = ~/.config/hypr/config/defaults.conf

# Autostart wiki https://wiki.hyprland.org/0.45.0/Configuring/Keywords/#executing #
exec-once = swww-daemon
# exec-once = waybar &                    # [REMOVED] replaced by AGS bar
# exec-once = mako &                      # [REMOVED] replaced by AGS notifications
# exec-once = swayosd-server &            # [REMOVED] replaced by AGS OSD
# exec-once = swayosd                     # [REMOVED] replaced by AGS OSD
exec-once = nm-applet --indicator &        # Network tray icon (AGS tray picks this up)
exec-once = ags run &                      # AGS v3 — bar, notifications, OSD, launcher, session
exec-once = fcitx5 -d &
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &

exec-once = wl-paste --watch cliphist store
exec-once = wl-clip-persist --clipboard regular

# ## Slow app launch fix
exec-once = systemctl --user import-environment &
exec-once = hash dbus-update-activation-environment 2>/dev/null &
exec-once = dbus-update-activation-environment --systemd &

# ## Idle configuration
exec-once = $idlehandler